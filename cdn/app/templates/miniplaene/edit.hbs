
<div class="small-12 large-9 columns">
	{{input type="text" class="headlineedit" value=internalTitle}}
	{{#each gd in gottesdienste}}
	{{render "gottesdienst" gd}}
	{{/each}}
</div>

<div class="hide-for-touch large-3 columns">
	<ul class="ministrant-drag-list">
		{{#each ministrants}}
		{{#view App.MinistrantDraggableTextView value=this}}
		{{shortenedName}}
		{{/view}}
		{{/each}}
	</ul>
</div>

<div class="small-12 columns show-for-touch leftoffcanvas">
	<select id="ministrantselectbox">
		{{#each ministrants}}
			<option {{bind-attr value=id}}>{{shortenedName}}</option>
		{{/each}}
	</select>
</div>
