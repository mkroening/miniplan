
<div class="small-12 large-9 columns">
	{{input type="text" class="headlineedit" value=internalTitle}}
	{{#each gd in gottesdienste}}
	{{render "gottesdienst" gd}}
	{{/each}}
</div>

<div class="small-12 large-3 columns">
	<ul class="ministrant-drag-list">
		{{#each ministrants}}
		<li draggable="true">{{shortenedName}}</li>
		{{/each}}
	</ul>
</div>
